<h1>Themes/index</h1>
<h3>Congrats, you made it to the index page</h3>
<ul>
  <% @themes.each do |theme| %>
    <li><%= link_to theme.name, theme_dreams_path(theme) %></li>
  <% end %>
</ul>

<%= link_to "Add New Theme", new_theme_path %>